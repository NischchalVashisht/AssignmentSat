def fabinocci(x:Array[Int],y:Int):Array[Int]= {

  val m=x.length
  println(m)
 
   if(y==x.length) x 
   else {
      //println("inside")
     fabinocci(x:+ x(x.length-2) + x(x.length-1),y)
   }
 }


val limit=8
val k=fabinocci(Array(0,1),limit)
for(i<-k) println(i)
